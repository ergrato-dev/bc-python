[project]
name = "studio-api-client"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "httpx>=0.27",
    "pydantic>=2.7",
    "tenacity>=8.3",
    "rich>=13.0",
]

[project.optional-dependencies]
dev = ["mypy>=1.10", "pytest>=8.0", "pytest-httpx>=0.30"]

[tool.mypy]
strict = true

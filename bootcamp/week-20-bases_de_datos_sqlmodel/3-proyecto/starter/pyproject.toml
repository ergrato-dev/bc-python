[project]
name = "studio-catalog-db"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "sqlmodel>=0.0.19",
    "alembic>=1.13",
    "rich>=13.0",
]

[project.optional-dependencies]
dev = ["mypy>=1.10", "pytest>=8.0"]

[tool.mypy]
strict = true

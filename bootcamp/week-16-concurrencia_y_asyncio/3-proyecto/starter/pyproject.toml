[project]
name = "week16-asset-pipeline"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "aiofiles>=24.1.0",
]

[tool.mypy]
strict = true
python_version = "3.13"

[tool.uv]
dev-dependencies = [
    "mypy>=1.11",
    "pytest>=8.0",
    "pytest-asyncio>=0.24",
]
